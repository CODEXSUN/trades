name: ${TRADES_COMPOSE_PROJECT:-trades}

services:
  verify:
    profiles: [tools]
    build:
      context: ..
      dockerfile: .container/scripts/Dockerfile.stack
      target: verify
      args:
        NODE_RUNTIME_VERSION: ${NODE_RUNTIME_VERSION:-26.5.0}
        NPM_RUNTIME_VERSION: ${NPM_RUNTIME_VERSION:-12.0.1}
    image: ${TRADES_IMAGE_REGISTRY:-trades}/verify:${TRADES_IMAGE_TAG:-local}

  mariadb:
    image: ${MARIADB_IMAGE:-mariadb:11.8}
    container_name: ${TRADES_MARIADB_CONTAINER_NAME:-trades-mariadb}
    restart: unless-stopped
    environment:
      MARIADB_DATABASE: ${DB_NAME:?DB_NAME is required}
      MARIADB_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD is required}
      MARIADB_ROOT_PASSWORD: ${MARIADB_ROOT_PASSWORD:?MARIADB_ROOT_PASSWORD is required}
      MARIADB_USER: ${DB_USER:?DB_USER is required}
    volumes:
      - mariadb-data:/var/lib/mysql
    networks: [trades]
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      start_period: 30s
      retries: 12

  api:
    build:
      context: ..
      dockerfile: .container/scripts/Dockerfile.stack
      target: api-runtime
      args:
        NODE_RUNTIME_VERSION: ${NODE_RUNTIME_VERSION:-26.5.0}
        NPM_RUNTIME_VERSION: ${NPM_RUNTIME_VERSION:-12.0.1}
    image: ${TRADES_IMAGE_REGISTRY:-trades}/api:${TRADES_IMAGE_TAG:-local}
    container_name: ${TRADES_API_CONTAINER_NAME:-trades-api}
    restart: unless-stopped
    env_file:
      - ${TRADES_RUNTIME_ENV_FILE:-../.env}
    environment:
      DEV_AUTO_LOGIN: "0"
      NODE_ENV: production
      PLATFORM_API_PORT: ${TRADES_API_INTERNAL_PORT:-7050}
      TRADES_ENV_FILE_PATH: /workspace/trades/.env
    depends_on:
      mariadb:
        condition: service_healthy
    ports:
      - "${TRADES_BIND_ADDRESS:-127.0.0.1}:${TRADES_API_HOST_PORT:-7050}:${TRADES_API_INTERNAL_PORT:-7050}"
    volumes:
      - ${TRADES_RUNTIME_ENV_FILE:-../.env}:/workspace/trades/.env:rw
    networks: [trades]
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:'+process.env.PLATFORM_API_PORT+'/health').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"
        ]
      interval: 10s
      timeout: 5s
      start_period: 90s
      retries: 18

  web:
    build:
      context: ..
      dockerfile: .container/scripts/Dockerfile.stack
      target: web-runtime
      args:
        NODE_RUNTIME_VERSION: ${NODE_RUNTIME_VERSION:-26.5.0}
        NPM_RUNTIME_VERSION: ${NPM_RUNTIME_VERSION:-12.0.1}
    image: ${TRADES_IMAGE_REGISTRY:-trades}/web:${TRADES_IMAGE_TAG:-local}
    container_name: ${TRADES_WEB_CONTAINER_NAME:-trades-web}
    restart: unless-stopped
    environment:
      NGINX_ENVSUBST_FILTER: TRADES_API
      TRADES_API_CONTAINER_NAME: ${TRADES_API_CONTAINER_NAME:-trades-api}
      TRADES_API_INTERNAL_PORT: ${TRADES_API_INTERNAL_PORT:-7050}
    depends_on:
      api:
        condition: service_healthy
    ports:
      - "${TRADES_BIND_ADDRESS:-127.0.0.1}:${TRADES_WEB_HOST_PORT:-7060}:80"
    networks: [trades]
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1/health >/dev/null || exit 1"]
      interval: 10s
      timeout: 5s
      start_period: 15s
      retries: 12

volumes:
  mariadb-data:
    name: ${TRADES_MARIADB_DATA_VOLUME:-trades-mariadb-data}

networks:
  trades:
    name: ${TRADES_NETWORK:-trades-network}
    external: ${TRADES_NETWORK_EXTERNAL:-false}
